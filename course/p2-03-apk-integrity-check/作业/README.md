# 2.3 作业：读懂重打包/签名校验的关键代码

## 题干

2.2 你把包重打包 + 重签了——那游戏怎么**发现**自己被重打包过？这一课让你**看关键代码**：一套自制的签名校验样例，演示主流移动端加固普遍的做法，读明白它到底怎么做。**flag 就藏在代码注释里。**

## 材料

面板上点 **⬇ 下载材料**，拿到 `sigcheck-src.zip`，解开读 `sigcheck.cpp`。

## 怎么交

塔里点这个节点 → **✍ 做作业** → 把注释里的 flag 填进去 → **▶ 运行批改**。
