# 3.3 作业：把 yield 状态机还原成真 C#，算出藏在里面的 flag

## 题干

点面板上的 **⬇ 下载材料**，拿到 `dump.cs`——这是爬塔游戏**通关时揭示奖励码的协程** `Puzzle33.RevealFlag()`，被 IL2CPP 编译成 C# 后 dump 出来的**状态机**（`<RevealFlag>d__1`，字段名全是 `<>`-mangled，解码逻辑散在 `MoveNext` 的每一次调用里），直接读几乎没法看。奖励码（flag）藏在这段协程逻辑里，把它还原成可读的 C#、算出 flag。

## 材料

| 文件 | 是什么 |
|---|---|
| `dump.cs` | 爬塔通关揭示奖励码的协程 `Puzzle33.RevealFlag()`，`yield` 被编译后的状态机 |

## 怎么交

塔里点这个节点 → **✍ 做作业** → 把还原+解码得到的 flag 填进去 → **▶ 运行批改**。
