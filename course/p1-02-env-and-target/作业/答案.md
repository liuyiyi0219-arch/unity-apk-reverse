> 先自己动手，卡住了再看。

### 一、一步步操作

先把靶子游戏 APK 装上：
```bash
adb install -r <游戏>.apk
```
然后到塔里点节点 → **✍ 做作业** → **▶ 运行环境检查**，三项全绿即过。

### 二、知识点解析

装好后可以先跑起来认认它：`adb logcat -s GameBoot Unity`，能看到伤害 `107` / 金币 `85` / 签名 / license / Lua 战斗 `231`——这些就是后面各章要逆的埋点。
